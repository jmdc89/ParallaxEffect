import React from "react";
import ReactDOM from "react-dom";
import ReactParallax from "./components/reactParallax";
import ParHooks from "./components/ParHooks";

ReactDOM.render(
  <React.StrictMode>
    {/* <ReactParallax /> */}
    <ParHooks />
  </React.StrictMode>,
  document.getElementById("root")
);
